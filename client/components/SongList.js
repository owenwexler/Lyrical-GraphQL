import React from 'react';

const SongList = ({ songs }) => {
  return (
    <div>

      <h1>SongList</h1>
      {/* <td>
        {
          songs.map(song => {
            return <tr><h1>{song.title}</h1></tr>
          })
        }
      </td> */}

    </div>
  )
}

export default SongList;
